# Copy this file to .env and adjust values
# Windows PowerShell:
#   Copy-Item .env.example .env
# macOS/Linux:
#   cp .env.example .env

# Required
# Async SQLAlchemy DSN (asyncpg driver)
# Format: postgresql+asyncpg://USER:PASS@HOST:PORT/DBNAME
DATABASE_URL=postgresql+asyncpg://app:app_password@localhost:5432/app_db

# Shared secret for webhook HMAC verification (header X-Signature)
WEBHOOK_SECRET=change-me-please

# Optional Redis configuration for idempotency (choose REDIS_URL or discrete vars)
# Combined URL form:
# REDIS_URL=redis://localhost:6379/0

# Discrete params (used if REDIS_URL is not provided)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_USERNAME=
# REDIS_PASSWORD=
# Enable TLS by setting to true/1/yes
REDIS_TLS=false
